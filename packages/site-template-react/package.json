{
  "name": "@dcms/site-template-react",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "The starter templates for DCMS Mode B sites, embedded into admin-api. Its build typechecks every template against shared/src/api, a committed copy of real generated client output.",
  "scripts": {
    "build": "tsc -p tsconfig.blank.json && tsc -p tsconfig.content.json && tsc -p tsconfig.landing.json"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "react": "^19",
    "react-dom": "^19",
    "react-router": "7.18.2",
    "typescript": "^5",
    "vite": "^7"
  }
}
