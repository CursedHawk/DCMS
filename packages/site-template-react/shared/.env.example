# Base URL of the tenant content API. Leave empty on a site DCMS builds and hosts: it is served
# from the tenant's own domain, which answers /api same-origin. Set it only for local development
# against a remote tenant, e.g. VITE_API_BASE_URL=https://acme.example
VITE_API_BASE_URL=
