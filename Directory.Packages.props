<Project>

  <PropertyGroup>
    <CentralPackageTransitivePinningEnabled>false</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup Label="AspNetCore / EF / Identity">
    <PackageVersion Include="Microsoft.AspNetCore.Authentication.Google" Version="10.0.9" />
    <PackageVersion Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.9" />
    <PackageVersion Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" Version="10.0.9" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.9" />
    <PackageVersion Include="Microsoft.AspNetCore.SignalR.StackExchangeRedis" Version="10.0.9" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.2" />
    <PackageVersion Include="Microsoft.Extensions.Caching.StackExchangeRedis" Version="10.0.9" />
    <PackageVersion Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.2" />
  </ItemGroup>

  <!-- Transitive floor raised for security. OpenIddict's *.DataProtection packages
       pull Microsoft.AspNetCore.DataProtection 10.0.7, which drags in
       System.Security.Cryptography.Xml 10.0.7 — flagged high severity by NuGet audit
       (CVE-2026-47302 / -47304 / -50525 / -50648), first patched in 10.0.10. Only
       Dcms.IntegrationTests resolves it as a package (the services get it from the
       shared framework), so it is referenced directly there to force the patched
       version. Drop both once OpenIddict raises its own floor. -->
  <!-- Transitive floor raised for security. Testcontainers (still 4.13.0) depends on
       SSH.NET 2025.1.0, which has a path-traversal flaw in ScpClient.Download
       (GHSA-q939-rpr3-3284, high severity), first patched in 2026.0.0. Only
       Dcms.IntegrationTests pulls Testcontainers, so SSH.NET is referenced directly
       there to force the patched version. Drop both once Testcontainers raises its
       own floor. -->
  <ItemGroup Label="Security pins">
    <PackageVersion Include="SSH.NET" Version="2026.0.0" />
    <PackageVersion Include="System.Security.Cryptography.Xml" Version="10.0.10" />
  </ItemGroup>

  <ItemGroup Label="Multi-tenancy / Auth">
    <PackageVersion Include="Finbuckle.MultiTenant.AspNetCore" Version="10.1.1" />
    <PackageVersion Include="Finbuckle.MultiTenant.EntityFrameworkCore" Version="10.1.1" />
    <PackageVersion Include="OpenIddict.AspNetCore" Version="7.5.0" />
    <PackageVersion Include="OpenIddict.EntityFrameworkCore" Version="7.5.0" />
  </ItemGroup>

  <ItemGroup Label="Infrastructure clients">
    <PackageVersion Include="DnsClient" Version="1.8.0" />
    <PackageVersion Include="MailKit" Version="4.17.0" />
    <PackageVersion Include="Minio" Version="7.0.0" />
    <PackageVersion Include="NATS.Net" Version="2.8.1" />
    <PackageVersion Include="StackExchange.Redis" Version="2.13.17" />
    <PackageVersion Include="VaultSharp" Version="1.17.5.1" />
    <PackageVersion Include="Yarp.ReverseProxy" Version="2.3.0" />
  </ItemGroup>

  <ItemGroup Label="Media">
    <PackageVersion Include="FFMpegCore" Version="5.4.0" />
    <!-- Pinned to 3.1.x: ImageSharp 4.x enforces a commercial license key at
         build time. 3.1 ships under the Six Labors Split License (free below
         the revenue threshold). Revisit in Phase 5 (docs/adr). -->
    <PackageVersion Include="SixLabors.ImageSharp" Version="3.1.12" />
  </ItemGroup>

  <ItemGroup Label="AI">
    <PackageVersion Include="Anthropic" Version="12.29.0" />
    <PackageVersion Include="OpenAI" Version="2.11.0" />
  </ItemGroup>

  <ItemGroup Label="OpenAPI / Schema">
    <PackageVersion Include="JsonSchema.Net" Version="9.2.1" />
    <PackageVersion Include="Microsoft.OpenApi" Version="3.7.0" />
    <PackageVersion Include="Scalar.AspNetCore" Version="2.16.3" />
    <PackageVersion Include="YamlDotNet" Version="16.3.0" />
  </ItemGroup>

  <ItemGroup Label="Observability">
    <!-- Matches the Npgsql that Npgsql.EntityFrameworkCore.PostgreSQL 10.0.2 resolves, so the
         driver is not pulled forward by the instrumentation package alone. -->
    <PackageVersion Include="Npgsql.OpenTelemetry" Version="10.0.2" />
    <PackageVersion Include="OpenTelemetry" Version="1.16.0" />
    <PackageVersion Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.16.0" />
    <PackageVersion Include="OpenTelemetry.Extensions.Hosting" Version="1.16.0" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.15.2" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Http" Version="1.15.1" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Runtime" Version="1.16.0" />
    <!-- Prerelease because there has never been a stable release of this one; it is the only
         way to get Redis command spans, and the alternative is a blind spot over the cache and
         the SignalR backplane. Process instrumentation is deliberately NOT taken: it is also
         prerelease-only and cAdvisor already reports per-container CPU and memory. -->
    <PackageVersion Include="OpenTelemetry.Instrumentation.StackExchangeRedis" Version="1.16.0-beta.1" />
    <PackageVersion Include="Serilog.AspNetCore" Version="10.0.0" />
    <PackageVersion Include="Serilog.Sinks.OpenTelemetry" Version="4.2.0" />
  </ItemGroup>

  <ItemGroup Label="Health checks">
    <PackageVersion Include="AspNetCore.HealthChecks.NpgSql" Version="9.0.0" />
    <PackageVersion Include="AspNetCore.HealthChecks.Redis" Version="9.0.0" />
  </ItemGroup>

  <ItemGroup Label="Resilience">
    <PackageVersion Include="Polly" Version="8.7.0" />
  </ItemGroup>

  <ItemGroup Label="Testing">
    <PackageVersion Include="AwesomeAssertions" Version="9.4.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.6.0" />
    <PackageVersion Include="NSubstitute" Version="5.3.0" />
    <PackageVersion Include="Respawn" Version="7.0.0" />
    <PackageVersion Include="Testcontainers.Minio" Version="4.12.0" />
    <PackageVersion Include="Testcontainers.Nats" Version="4.12.0" />
    <PackageVersion Include="Testcontainers.PostgreSql" Version="4.12.0" />
    <PackageVersion Include="Testcontainers.Redis" Version="4.12.0" />
    <PackageVersion Include="xunit.v3" Version="3.2.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
  </ItemGroup>

</Project>
