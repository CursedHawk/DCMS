# FIXES, CHANGES, AND ADDITIONS:
```
Monaco mode B
    - Enable folder move and deletion 
Grapejs mode A
    - Improve handling of custom components when they pull data lists (moordoor events, crew members, etc.)
    - Improve the ability to click through to an item from a list (moordoor event, crew member, etc.)
    - Enable clicking on Git diffs and showing diff
    - Resizable split view for editing (Grape x Monaco)
    - Add a document to Git and to every website with instructions for the AI on how to generate a website for our GrapeJS implementation (the document must be there automatically and permanently)
    - Add styling to the scroll slider
Tag system 
    - add already existing tags selection from dropdown in admin spa when tenant already have some tags elsewhere. Everywhere where tags can selected. So tags are reusable and filtrable. 
    - And than serve on API edpoint existing tenant tags and where they occurs in tenant/api/entity so it is filtrable and "browseable". Add propper descriptions for openapi deocument. Only when tags are present and used on tenant. 
```